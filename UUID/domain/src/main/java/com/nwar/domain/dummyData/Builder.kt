package com.nwar.domain.dummyData

import com.nwar.domain.entity.StoreInfo
import com.nwar.domain.entity.SurveyQuestion

/*
fun getQuestion(id : Int = 0) = SurveyQuestion("질문질문")

fun getQuestionList() = arrayListOf<SurveyQuestion>().apply { (0..10).forEach { add(getQuestion(it)) } }

fun getSaleInfo(id : Int = 0) = StoreInfo(id, "", "스타벅스 OO점", "음료 10% 할인", 5)

fun getSaleInfoList() = arrayListOf<StoreInfo>().apply { (0..10).forEach { add(getSaleInfo(it)) } }
 */